import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  define: {
    // Injects the production Render WebSocket URL directly into the client bundle
    'import.meta.env.VITE_RENDER_WS_URL': JSON.stringify('wss://medivoice-ai-1kpx.onrender.com'),
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
})
